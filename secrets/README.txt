Private keypair JSON backups may live here (chmod 600).
Never commit this directory.

Runtime secrets: use a single root .env (see .env.example).
Do not put private keys in chat, HTTP forms, or screenshots.
